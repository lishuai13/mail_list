<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lishuai.mapper.SpecialitiesMapper">

    <select id="select" resultType="java.lang.String">
        select specialities_name from specialities where specialities_id=#{specialitiesId};
    </select>
</mapper>